<%- include('header.ejs') %>

  <h1>
    <%= title %>
  </h1>
  <a class="item btn" href="/new">Add task</a>
  <div class="container">
    <ul>

      <% todo.forEach(function(task) { %>
        <div class="itens">
          <li><a class="item" href="/edit/<%= task._id %>">
              <%= task.task %>
            </a><a class="item del-btn" href="/delete/<%= task._id %>" onclick="return confirm('Deseja excluir?');"><img
                class="trash-icon" src="/images/trash.png" alt=""></a><a class="item del-btn" href="/delete/<%= task._id %>" onclick="return confirm('Tarefa Concluida?');"><img
                  class="trash-icon" src="/images/done.png" alt=""></a>
            
              <p>
                <%= task.desc %>
              </p>
              
          </li>
        </div>
        <% }); %>

    </ul>
  </div>
  </div>
 